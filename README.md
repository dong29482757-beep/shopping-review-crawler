# 쇼핑몰 리뷰 크롤러

> 본 프로젝트는 대학교 수업 과제로 진행된 학술 연구 목적의 데이터 수집 프로젝트입니다.  
> 수집된 데이터는 텍스트 분석 연구 외의 용도로 사용하지 않습니다.

쿠팡 화장품 카테고리 리뷰 데이터를 수집하고 전처리하는 파이프라인 프로젝트입니다.  
수집된 데이터는 **BeautyScope(화장품 리뷰 분석 웹 서비스)** 와 **Streamlit 기반 데이터 시각화 대시보드** 에 활용됩니다.

---

## 수집 현황

| 사이트 | 키워드 수 | 상품 수 | 원본 리뷰 수 | 전처리 후 | 수집일 |
|--------|-----------|---------|------------|----------|--------|
| 쿠팡 | 20개 | 543개 | 112,971건 | **86,380건** | 2026-05-14 |

### 키워드 목록 (20개)
화장품, 스킨케어, 마스크팩, 선크림, 토너, 세럼, 클렌징폼, 파운데이션, 립스틱, 아이크림,  
BB크림, 쿠션팩트, 컨실러, 수분크림, 클렌징오일, 립틴트, 아이섀도, 마스카라, 나이트크림, 미스트

### 최종 데이터 통계 (전처리 후)
| 항목 | 값 |
|------|-----|
| 총 리뷰 수 | 86,380건 |
| 수집 기간 | 2015-06-28 ~ 2026-05-14 |
| 상품 수 | 543개 |
| 키워드 수 | 20개 |

### 별점 분포
| 별점 | 건수 | 비율 |
|------|------|------|
| 1점 | 6,095건 | 7.1% |
| 2점 | 1,984건 | 2.3% |
| 3점 | 2,287건 | 2.6% |
| 4점 | 7,805건 | 9.0% |
| 5점 | 68,209건 | 79.0% |

### 감성 레이블 분포
| 감성 | 기준 | 건수 | 비율 |
|------|------|------|------|
| positive | 4~5점 | 76,014건 | 88.0% |
| negative | 1~2점 | 8,079건 | 9.3% |
| neutral | 3점 | 2,287건 | 2.6% |

---

## 데이터 구조

최종 파일: `coupang_reviews_final.csv` (86,379건)

| 컬럼 | 타입 | 설명 | 예시 |
|------|------|------|------|
| `review_id` | int | 리뷰 고유 ID | `892643279` |
| `product_id` | int | 상품 고유 ID | `8633754180` |
| `product_name` | str | 상품명 | `닥터지 블랙 스네일 크림 듀오, 50ml, 2개` |
| `keyword` | str | 검색 키워드 | `화장품` |
| `rating` | int | 별점 (1~5) | `5` |
| `sentiment` | str | 감성 레이블 | `positive` |
| `review_title` | str | 리뷰 제목 (없으면 빈 문자열) | `피부가 좋아졌어요` |
| `review_content` | str | 리뷰 본문 | `한 달째 쓰고 있는데 ...` |
| `review_date` | str | 작성일 (YYYY-MM-DD) | `2026-05-10` |
| `nickname` | str | 작성자 닉네임 | `다시fc` |

### 감성 레이블 기준
| sentiment | rating | 의미 |
|-----------|--------|------|
| `positive` | 4 ~ 5점 | 긍정 리뷰 |
| `neutral` | 3점 | 중립 리뷰 |
| `negative` | 1 ~ 2점 | 부정 리뷰 |

---

## 데이터 장점

### 1. 즉시 활용 가능한 레이블 보유
별점 기반 감성 레이블(`sentiment`)이 이미 부여되어 있어 별도 수작업 레이블링 없이 **감성 분류 모델 학습에 바로 사용** 가능합니다.

### 2. 도메인 특화 (화장품)
화장품 카테고리에 집중된 데이터로, 일반 리뷰 데이터 대비 **도메인 특화 분석에 유리**합니다.  
성분, 제형, 향, 지속력 등 화장품 특유의 표현이 풍부하게 포함되어 있습니다.

### 3. 장기간 데이터 (2015~2026)
10년치 리뷰 데이터를 포함해 **트렌드 분석, 시계열 분석**이 가능합니다.

### 4. 키워드 분류 체계 보유
20개 카테고리 키워드로 분류되어 있어 **카테고리별 비교 분석**이 용이합니다.

### 5. 노이즈 제거 완료
체험단·협찬·도배 리뷰를 제거해 **실구매자 리뷰만** 남아있습니다.

---

## 활용 방안

### BeautyScope (Spring + MySQL)
```
coupang_reviews_final.csv
        ↓ MySQL import
┌─────────────────────────────┐
│  키워드별 리뷰 검색         │
│  상품별 감성 통계 조회      │
│  평점 분포 시각화           │
│  부정 리뷰 키워드 추출      │
└─────────────────────────────┘
```

### Streamlit 데이터 분석 대시보드
```
coupang_reviews_final.csv
        ↓ 직접 로드
┌─────────────────────────────┐
│  키워드별 리뷰 수 비교      │
│  연도별 리뷰 트렌드         │
│  감성 분포 차트             │
│  워드클라우드               │
└─────────────────────────────┘
```

### 감성 분류 모델 학습
```
coupang_reviews_final.csv
        ↓ review_content + sentiment 사용
┌─────────────────────────────┐
│  positive / negative 이진 분류  │
│  KoELECTRA 파인튜닝         │
│  또는 TF-IDF + SVM          │
└─────────────────────────────┘
```

---

## 데이터 전처리 파이프라인

```
[원본 CSV 2개]
coupang_reviews_20260514_001626.csv  (61,164건)
coupang_reviews_20260514_224303.csv  (51,807건)
        ↓
[1단계] 병합 → 112,971건
[2단계] 중복 제거 (review_id 기준) → 108,022건 (-4,949건)
[3단계] 텍스트 정제 (줄바꿈·공백 정규화)
[4단계] 본문 10자 미만 제거 → 107,193건 (-829건)
[5단계] 감성 레이블 추가 (rating 기반)
[6단계] 날짜 정규화 (Excel 시리얼 → YYYY-MM-DD)
[7단계] 체험단·협찬 리뷰 제거 → 87,409건 (-19,784건)
[8단계] 도배 리뷰 제거 (본문 앞 50자 기준) → 86,380건 (-1,029건)
        ↓
[최종] coupang_reviews_final.csv (86,380건)
```

### 전처리 주요 이슈 및 해결

| 이슈 | 원인 | 해결 |
|------|------|------|
| 날짜가 1970-01-01로 표시 | 크롤러가 날짜를 Excel 시리얼 넘버로 저장 | `1899-12-30` 기준 timedelta 변환 |
| 체험단 리뷰 18.5% 포함 | "쿠팡체험단 이벤트로 상품을 무료 제공받아" 등 문구 | 키워드 필터링으로 19,784건 제거 |
| 도배 리뷰 존재 | 동일한 체험단 문구 복붙 | 본문 앞 50자 기준 중복 제거 |

---

## 기술 스택

- **Python 3.14**
- **Playwright** — 브라우저 자동화 (CDP 연결 방식)
- **BeautifulSoup4** — HTML 파싱
- **pandas** — 데이터 처리 및 전처리
- **tqdm** — 진행률 표시

---

## 쿠팡 차단 우회 방법

쿠팡은 **Akamai Bot Manager** 를 사용해 일반적인 requests나 Selenium으로는 바로 차단됩니다.

### 왜 일반 방식이 안 되는가

Akamai는 5단계로 봇을 탐지합니다.

1. HTTP 헤더, TLS 지문 분석
2. `_abck` 쿠키 기반 세션 검증
3. `navigator.webdriver` 등 브라우저 자동화 흔적 탐지
4. 마우스/키보드/스크롤 패턴 분석
5. IP 평판, 요청 빈도

requests는 1단계에서, Selenium은 3단계에서 차단됩니다.

### 해결 방법: 실제 Chrome + CDP 연결

```
실제 Chrome 실행 (--remote-debugging-port=9222)
       ↓
기존 Chrome 프로필 쿠키를 임시 폴더에 복사
       ↓
Playwright가 connect_over_cdp()로 연결
       ↓
검색창에 키워드를 한 글자씩 타이핑 (URL 직접 접속 X)
```

### 리뷰 데이터 수집 방식

HTML 파싱 대신 **브라우저 내부 API 응답을 가로채는 방식** 사용

```
상품 페이지 접속 → 상품평 탭 클릭
       ↓
브라우저가 내부 API 자동 호출
https://www.coupang.com/next-api/review?productId=...
       ↓
Playwright response interceptor로 JSON 응답 캡처
       ↓
rData → paging → contents 경로로 리뷰 추출
```

2페이지부터는 `page.request.get()`으로 직접 호출하며,  
별점 필터(`ratings=1`, `ratings=2`)를 별도 요청해서 낮은 별점 리뷰도 일정 수 이상 확보합니다.

### 차단 방지 장치

- 상품 사이 랜덤 딜레이 (2~4초)
- 상품 5개마다 쿠팡 홈 방문 + 15~30초 휴식
- 키워드 전환마다 1~2분 대기
- 랜덤 스크롤로 사람처럼 행동 시뮬레이션

---

## 파일 구조

```
crolling/
├── coupang_crawler.py    # 메인 크롤러 클래스
├── run_test.py           # 테스트 실행 (상품 1개, 리뷰 30건)
├── run_full.py           # 1차 본격 수집 (키워드 10개)
├── run_more.py           # 2차 추가 수집 (키워드 10개 확장)
├── preprocess.py         # 전처리 메인 (병합 → 정제 → 저장)
├── remove_noise.py       # 체험단·도배 리뷰 제거
├── validate.py           # 데이터 품질 검증
├── quality_check.py      # 정확성·노이즈 심층 체크
└── README.md
```

> CSV 파일은 용량 문제로 .gitignore에서 제외됩니다.

---

## 실행 방법

```bash
# 가상환경 활성화
.\coupang_env\Scripts\activate

# 1. 테스트 실행 (Chrome 완전히 닫은 상태에서)
python run_test.py

# 2. 본격 수집
python run_full.py

# 3. 전처리 (수집 완료 후)
python preprocess.py
python remove_noise.py

# 4. 품질 검증
python validate.py
python quality_check.py
```

> Chrome을 완전히 종료한 상태에서 실행해야 합니다.

---

## 데이터 품질 평가

| 항목 | 평가 | 비고 |
|------|------|------|
| 완전성 | ✅ 양호 | 결측값 0건 |
| 정확성 | ✅ 양호 | 평점-감성 100% 일치 |
| 일관성 | ✅ 양호 | 날짜·텍스트 형식 통일 |
| 대표성 | ⚠️ 주의 | 5점 리뷰 편향 (79%), 쿠팡 리뷰 특성상 불가피 |
| 노이즈 | ✅ 처리 완료 | 체험단 19,784건 + 도배 1,029건 제거 |
| 활용 가능성 | ✅ 즉시 사용 가능 | BeautyScope · Streamlit 대시보드 연동 예정 |

---

## BeautyScope 서비스 (쿠팡+무신사+올리브영 통합)

쿠팡 단독 크롤링 데이터(86,379건)에서 시작해서, 동료가 크롤링한 무신사·
올리브영 데이터까지 더 모아 **538,774건** 규모로 키운 뒤, 그 데이터로
감성 분류 모델(ML+DL)을 학습하고 Streamlit 서비스(BeautyScope)로 구현한
프로젝트입니다. 5분 발표용 슬라이드는 [BeautyScope_발표.pptx](BeautyScope_발표.pptx),
단계별 문제/해결 과정 전체는 [DEVLOG.md](DEVLOG.md)에 정리되어 있습니다.

> `konlpy`(Okt 형태소분석)는 내부적으로 JVM을 쓰므로 Java(JRE 8+)가 설치되어 있어야 합니다.

### 어떤 문제를 ML/DL로 풀었나

화장품 리뷰는 한 상품에 수백~수천 건씩 쌓여서 사람이 다 읽고 판단하기
어렵고, 평점만 보면 "왜" 좋고 나쁜지, 그리고 보습력/트러블/향처럼 **속성별로
어떤지**는 알 수 없다. 그래서 두 가지를 ML/DL로 해결했다.

1. **감성 분류** — 리뷰 텍스트 → 긍정/중립/부정 (ML: TF-IDF+로지스틱회귀,
   DL: numpy로 직접 구현한 신경망 + 추가로 PyTorch LSTM/KoELECTRA 비교실험)
2. **속성기반 감성분석(ABSA)** — 리뷰를 속성별(보습/향/트러블 등)로 쪼개서
   "이 상품은 보습력은 좋은데 향은 별로다" 같은 세부 정보를 추출

### 데이터 수집 — 처음엔 쿠팡만, 그다음 더 모았다

| 단계 | 데이터 | 건수 |
|---|---|---|
| 1차 | 쿠팡 크롤링 (20개 키워드) | 86,379건 |
| 2차 | + 동료의 무신사 크롤링 데이터 추가 | 230,870건 |
| 2차 | + 동료의 올리브영 크롤링 데이터 추가 (JSONL 4개 파일) | 221,525건 |
| **최종** | 스키마/라벨 통일 + 중복 제거 후 통합 | **538,774건** |

> 1차 쿠팡 크롤링 자체도 순탄하지 않았다 — 쿠팡은 Akamai Bot Manager로
> requests/Selenium을 자동 차단해서, 실제 Chrome을 Playwright로 원격
> 연결하는 방식으로 우회해야 했다. 자세한 내용은 [위 "쿠팡 차단 우회
> 방법"](#쿠팡-차단-우회-방법) 참고.

### 데이터 준비 단계 — 가장 까다로웠던 부분

데이터를 늘리는 것 자체는 쉬웠지만, **3개 플랫폼을 하나로 합치는 과정에서
예상 못한 문제들**이 계속 나왔다. 단계별로 겪은 문제와 해결:

| 단계 | 겪은 문제 | 해결 방법 |
|---|---|---|
| 스키마 통합 | 쿠팡/무신사/올리브영 컬럼명이 전부 다름 (CSV 2종 + JSONL 4파일) | 플랫폼별 로더 함수를 따로 작성해 공통 스키마로 변환 후 병합 (`merge_datasets.py`) |
| **라벨 기준 통일 (가장 큰 문제)** | 무신사의 `sentiment` 컬럼이 별점이 아니라 텍스트 분석 기반이라, 5점 리뷰의 21%가 "부정"으로 라벨링되어 있었음 | 전체 데이터의 sentiment를 별점에서 일괄 재계산해 기준 통일 |
| 인코딩 | 무신사 CSV를 기본 인코딩으로 읽으면 한글이 깨짐(mojibake) | `encoding='utf-8-sig'` 명시 |
| 중복 | 같은 리뷰가 여러 크롤링 시점에 중복 수집됨 | `product_id+nickname+date+content` 기준 중복 제거 (1,085건) |
| 클래스 불균형 | positive 86.7% — 그대로 학습하면 다 positive로 찍어도 86% 정확도 | negative/neutral 전량 + positive는 2배 규모로만 다운샘플링 |
| **불용어/형태소 미처리** | 정규식으로 음절만 잘라내서 조사가 토큰에 섞임("좋아요"≠"좋아서") | Okt 형태소분석 + 어간 정규화 + 불용어 제거 (`preprocessing_ko.py`) |

### AI(Claude Code)로 어떻게 구현했나

전처리 스크립트, 모델 코드, Streamlit 앱을 Claude Code와 페어 프로그래밍
방식으로 작성했다. 특히 도움이 됐던 부분:
- 라벨 불일치를 "느낌"이 아니라 `pd.crosstab(rating, sentiment)` 코드를
  직접 돌려 수치(5점인데 부정 21%)로 확인 → 문제를 정확히 진단
- 환경 제약(PyTorch 미설치)을 만났을 때 numpy 직접 구현 → 별도 Python
  3.11 venv 구성 → LSTM/Transformer 실험까지, 막힐 때마다 대안을 같이 설계
- 모델을 만들고 끝내지 않고, 실제 운영 분포로 재평가해서 숨어있던
  neutral precision 0.20 문제를 찾아내고 보정

### 신경망 구성과 학습

**DL (numpy 직접 구현)**: TF-IDF(형태소 토큰, 15,000차원) → Dense(128, ReLU)
→ Dense(64, ReLU) → Dense(3, Softmax). 프레임워크 없이 순전파/역전파를
직접 구현 (PyTorch가 Python 3.14 환경에 설치되지 않아서). Mini-batch SGD +
momentum(0.9), learning rate 0.08 → epoch마다 0.92배 감소, batch 256, 15 epoch.

**추가 실험 (PyTorch, 별도 Python 3.11 venv)**: LSTM(Embedding→BiLSTM→Dense)과
KoELECTRA-small 파인튜닝까지 시도해서 구조 간 성능을 직접 비교했다.

### 모델 성능 — 밸런싱 학습셋 vs 실제 운영 분포

| | accuracy | macro F1 | 학습 데이터 |
|---|---|---|---|
| ML (형태소 토큰 TF-IDF + LogisticRegression) | 0.730 | 0.668 | 136,150건 |
| DL (numpy 직접구현 신경망) | 0.754 | 0.664 | 136,150건 |
| LSTM (PyTorch) | 0.734 | 0.671 | 136,150건 |
| KoELECTRA-small 파인튜닝 | 0.772 | 0.660 | 20,000건 (CPU 한계로 서브샘플) |

**문제 발견**: 위 수치는 클래스 밸런싱한 데이터 기준이라, 실제 운영 비율
(positive 86.7%)로 평가하니 neutral precision이 0.20까지 떨어지는 문제가
있었다 (label shift). `model_utils.py`에 클래스 사전확률 보정(prior
correction)을 추가해 재학습 없이 개선:

| | accuracy (실제 분포) | macro F1 (실제 분포) |
|---|---|---|
| ML 보정 후 | **0.909** | **0.675** |
| DL 보정 후 | **0.920** | **0.711** |

자세한 진단/원인/해결 과정은 [DEVLOG.md](DEVLOG.md) "모델 문제점과 개선" 참고.

### 서비스 — 텍스트 데모에서 "상품 리포트"로 전환, 그다음 더 구체화

처음 버전은 "리뷰 텍스트를 입력하면 감성을 알려주는" 데모였는데, 실제
구매를 고민하는 사람에게는 쓸모가 없다는 피드백을 받았다. **상품 리포트**
기능으로 전환: 상품을 검색하면 8개 속성(보습/수분, 향, 트러블/자극,
발림성/흡수력, 지속력, 가격/가성비, 용기/디자인, 효과)별 긍정 비율과
대표 리뷰를 보여준다. 538,774건 전체에 ML 모델(Okt 형태소분석 필요)을
돌리기엔 느려서, 정규식 키워드 매칭 기반의 규칙 기반 속성분석(`absa.py`)
으로 전체 데이터를 52초에 처리했다.

여기서 한 번 더 "조금 더 구체적이고 실용적이었으면 좋겠다"는 피드백을
받고, 데이터 대시보드와 리뷰 텍스트 데모 탭을 빼고 ABSA 데이터를 더
적극적으로 쓰는 기능 두 개를 추가했다 (`report_data.py`):

- **속성별 랭킹**: 카테고리+속성을 고르면 그 속성이 가장 좋은 상품
  Top 10을 보여준다 ("보습력 좋은 토너 Top 10")
- **대안 상품 추천**: 지금 보는 상품의 가장 약한 속성을 찾아서, 같은
  카테고리에서 그 속성이 더 나은 상품을 추천 ("이거 말고 다른 건?").
  절대 기준(긍정비율 80% 이상)으로 후보를 거르면 속성마다 기본 수준이
  달라서(트러블/자극은 언급 자체가 불만 맥락이라 전체 평균이 낮음) 후보가
  거의 안 나오는 문제가 있었음 → "현재 상품보다 충분히 나은가"(+10%p
  이상)로 기준을 바꿔서 해결
- **피부타입별 세그먼트 분석**: 무신사 원본에 있던 `skin_type`(피부타입)
  필드를 스키마 통합 과정에서 버렸었는데, 다시 추출해서 건성/지성/복합성/
  민감성별로 속성 평가를 따로 보여준다 (`precompute_skin_segments.py`,
  147,966건에서 피부타입 식별, 1,202개 상품×피부타입 세그먼트)

예: 어떤 토너는 전체 평점이 4.9인데 트러블/자극 속성만 보면 부정 비율이
67% — 평점만 봐서는 알 수 없는 정보를 드러낸다. 자세한 내용은
[DEVLOG.md](DEVLOG.md) "문제 2", "서비스 고도화" 참고.

### 배포 준비

- `requirements.txt`로 의존성 고정, 학습된 모델/집계 데이터를 `models/`에
  포함해 별도 학습 없이 바로 서비스 구동 가능
- 538,774건 원본 대신 사전 집계 CSV(`agg_*.csv`, `product_summary.csv` 등)를
  서비스가 읽도록 분리해 로딩 속도 확보
- 대용량 원본 크롤링 CSV는 `.gitignore` 처리, 정제된 `merged_reviews_all.csv`와
  모델/집계 파일만 추적

### 실행 방법

```bash
# 1. 데이터 통합
python merge_datasets.py

# 2. 학습 데이터 준비 (클래스 밸런싱) + 형태소 토큰화
python ml/prepare_data.py
python ml/tokenize_data.py

# 3. 모델 학습
python ml/train_ml.py
python dl/train_dl.py

# 4. 상품 리포트용 집계 생성 (속성분석 + 피부타입 세그먼트)
python precompute_absa.py
python precompute_skin_segments.py

# 5. 서비스 실행 (fileWatcherType none 권장 — 불필요한 재시작/연결끊김 방지)
streamlit run app.py --server.fileWatcherType none
```

### 추가 파일 구조

```
crolling/
├── merge_datasets.py       # 3개 플랫폼 데이터 통합 (스키마/라벨 통일)
├── preprocessing_ko.py      # Okt 형태소분석 + 불용어 제거 (지연 초기화)
├── absa.py                  # 규칙 기반 속성기반 감성분석(ABSA)
├── precompute_absa.py       # 상품별 속성 감성 집계 생성
├── precompute_skin_segments.py # 무신사 피부타입별 속성 감성 집계 생성
├── report_data.py           # 상품 리포트용 랭킹/대안추천/세그먼트 로직
├── ml/
│   ├── prepare_data.py     # 클래스 밸런싱, train/test 분리
│   ├── tokenize_data.py    # Okt 형태소 토큰화
│   └── train_ml.py         # TF-IDF + LogisticRegression
├── dl/
│   ├── neural_net.py       # numpy 기반 피드포워드 신경망 구현
│   ├── train_dl.py         # DL(FFNN) 모델 학습
│   ├── train_lstm.py       # PyTorch LSTM 학습 (torch_env에서 실행)
│   └── train_transformer.py # KoELECTRA 파인튜닝 (torch_env에서 실행)
├── precompute_dashboard.py # (현재 앱에는 미사용, 키워드 집계 스크립트 보존)
├── model_utils.py          # ML/DL 추론 유틸 (현재 앱 UI에는 미연결, 평가용)
├── app.py                  # Streamlit 서비스 (상품 리포트 / 속성별 랭킹 / 소개)
├── models/                 # 학습된 모델 + 집계 데이터
├── DEVLOG.md                # 개발 기록 (문제/해결 과정 상세)
└── BeautyScope_발표.pptx    # 5분 발표용 슬라이드
```
