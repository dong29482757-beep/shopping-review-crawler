# BeautyScope 개발 기록

---

## 뭘 만들었나

쿠팡·무신사·올리브영 화장품 리뷰 538,774건을 모아서  
상품별로 속성(보습, 향, 트러블/자극 등) 장단점을 분석해주는 서비스를 만들었다.

---

## 데이터 모으기

쿠팡은 직접 크롤링했고, 무신사·올리브영은 팀원 데이터를 합쳤다.  
세 군데를 하나로 합치는 게 생각보다 복잡했다.

제일 골치 아팠던 건 **무신사 감성 라벨 문제**였다.  
무신사 데이터에 긍정/부정 라벨이 이미 있어서 그냥 쓰려 했는데,  
5점짜리 리뷰 중 "부정"으로 붙은 게 42,000건이 넘었다.  
결국 세 군데 전부 별점 기준으로 다시 통일했다.

| 플랫폼 | 건수 |
|---|---|
| 무신사 | 230,870 |
| 올리브영 | 221,525 |
| 쿠팡 | 86,379 |
| 합계 | 538,774 |

---

## 모델 만들기

Python 3.14라서 PyTorch가 설치가 안 됐다. 공식 패키지에 3.14용이 없었다.  
어쩔 수 없이 numpy만 써서 신경망을 직접 구현했다.  
나중에 Python 3.11을 따로 깔고 PyTorch도 돌렸다 (GPU가 오래돼서 CPU로만).

| 모델 | 정확도 |
|---|---|
| TF-IDF + 로지스틱 회귀 | 0.738 |
| numpy 직접 구현 신경망 | 0.762 |
| LSTM (PyTorch) | 0.734 |
| KoELECTRA | 0.772 (데이터 1/7만 씀) |

---

## 상품 리포트 만들기

상품 이름으로 검색하면 속성별 장단점을 보여주는 서비스를 만들었다.  
보습, 향, 트러블/자극, 발림성, 지속력, 가성비, 용기, 효과 — 8가지 항목별 긍정 비율.

전체 평점 4.9인데 트러블/자극 항목 긍정률이 33%인 상품이 실제로 있다.  
별점만 봐선 알 수 없는 정보다.

속성 분석은 키워드 매칭 방식으로 만들었다.(`absa.py`)  
ML 모델로 전체 53만 건을 돌리면 Okt 형태소 분석이 병목이라 너무 오래 걸리고,  
키워드 매칭으로 하면 52초에 처리가 됐다.

---

## 피드백: "모델을 쓰긴 했는데 설득력이 없다"

상품 리포트를 만들고 나서 처음엔 학습된 모델을 **리뷰 신뢰도** 지표에 썼다.  
전체 리뷰에 모델을 돌려서 "텍스트 내용이 별점이랑 안 맞는다"고 판단된 비율을 상품별로 보여주는 것.

그런데 피드백이 왔다. **"모델이 리뷰 신뢰도 수치를 보여주는 데만 쓰이는 건 설득력이 떨어진다"**는 것이었다.  
신뢰도 점수 하나 더 추가되는 게 모델을 활용했다고 보기엔 약하다는 말이었다.

맞는 말이었다. 모델이 있어도 없어도 서비스에 보이는 핵심 숫자(속성별 긍정 비율, 랭킹)는 달라지지 않으니까.

---

## 대응: 모델이 핵심 숫자를 직접 바꾸게

리뷰 신뢰도 표시를 없애는 대신, 모델을 속성 집계 계산 자체에 연결했다.

모델이 "별점과 리뷰 내용이 안 맞는다"고 판단한 리뷰 **50,806건(전체의 9.5%)** 을  
속성별 긍정 비율 계산에서 아예 빼버렸다.  
나머지 488,000건으로 다시 계산한 숫자를 화면에 보여준다.

앱 상단에 "이 숫자는 50,806건을 제외하고 계산됐습니다"가 뜬다.  
**모델을 끄면 다른 숫자가 나온다.** 모델이 결과에 직접 영향을 주는 구조다.

---

## 중간에 고친 것들

**형태소 분석**  
"좋아요", "좋아서", "좋은데"가 전부 다른 단어로 취급되는 문제를 Okt로 해결했다.  
모델 정확도엔 별 차이 없었는데, 키워드 집계가 훨씬 보기 좋아졌다.

**검색 개선**  
카테고리 필터, 브랜드명 검색, 여러 단어 AND 검색을 추가했다.  
무신사/올리브영은 카테고리 컬럼이 없거나 대분류만 있어서  
상품명 키워드로 추정하는 방식으로 채웠다.

**Streamlit 연결 끊기는 문제**  
앱을 열면 "연결 중..." 배너가 계속 떴다.  
Okt를 불러올 때 자바가 켜지면서 10초 정도 멈추는 게 원인이었다.  
처음 실제로 쓸 때만 켜도록 바꿨더니 해결됐다.
