# BeautyScope 개발 기록

---

## 뭘 만들었나

쿠팡·무신사·올리브영 화장품 리뷰 538,774건을 모아서  
리뷰 감성을 자동으로 분류하는 모델을 만들고, 그걸 실제 서비스로 연결했다.

---

## 데이터 모으기

쿠팡은 직접 크롤링했다. 무신사·올리브영은 같이 프로젝트하던 팀원 데이터를 합쳤다.  
세 군데 데이터를 하나로 합치는 게 생각보다 훨씬 복잡했다.

제일 골치 아팠던 건 **무신사 감성 라벨 문제**였다.  
무신사 데이터에 긍정/부정 라벨이 이미 붙어있어서 그냥 쓰면 되겠다 싶었는데,  
5점짜리 리뷰 중에 "부정"이라고 라벨 붙은 게 42,000건 넘게 나왔다.  
라벨을 믿을 수 없으니 세 군데 전부 별점 기준으로 다시 통일했다.  
(4~5점=긍정, 3점=중립, 1~2점=부정)

| 플랫폼 | 건수 |
|---|---|
| 무신사 | 230,870 |
| 올리브영 | 221,525 |
| 쿠팡 | 86,379 |
| 합계 | 538,774 |

---

## 모델 만들기

### PyTorch가 안 깔렸다

처음엔 LSTM이나 Transformer 계열을 쓰려고 했는데  
이 컴퓨터 Python이 3.14라서 PyTorch가 설치 자체가 안 됐다.  
공식 패키지에 3.14용이 아직 없었다.

그래서 **numpy만 써서 신경망을 직접 구현**했다.  
손으로 역전파 짜면서 내부가 어떻게 돌아가는지 오히려 더 잘 이해했다.

나중에 Python 3.11을 따로 설치해서 별도 환경 만들고 PyTorch도 결국 돌렸다.  
GPU가 있긴 한데 드라이버가 너무 오래돼서 최신 버전이랑 안 맞아 CPU로 학습.

### 4개 모델 비교

| 모델 | 정확도 | 비고 |
|---|---|---|
| TF-IDF + 로지스틱 회귀 | 0.738 | 학습 6.6초 |
| numpy 직접 구현 신경망 | 0.762 | 학습 132초 |
| LSTM (PyTorch) | 0.734 | 사전학습 임베딩 없음 |
| KoELECTRA | 0.772 | 데이터 20,000건만 씀 |

LSTM이 TF-IDF 모델이랑 성능이 거의 똑같았다.  
짧은 화장품 리뷰는 단어 순서보다 "재구매", "별로", "최악" 같은 단어 자체가 더 강한 신호였던 것 같다.

KoELECTRA는 데이터를 1/7만 쓰고도 제일 높았다.  
CPU라 시간이 너무 걸려서 전체 학습은 못 했는데, GPU 환경이었으면 격차가 더 벌어졌을 거다.

---

## 핵심 피드백과 대응

### "모델을 쓰긴 했는데 실용성이 없다"

이 피드백이 가장 중요했다.

맞는 말이었다. 처음 만든 서비스는 "리뷰 텍스트를 직접 입력하면 긍정/부정을 알려주는" 데모였는데,  
생각해보면 이미 써본 리뷰를 직접 타이핑해서 모델한테 물어볼 이유가 없다.  
모델을 만들어놓고 쓸모없는 데 쓴 거였다.

그래서 서비스를 처음부터 다시 설계했다.

---

## 새로 만든 것: 상품 리포트

상품 이름으로 검색하면 이런 걸 보여준다.

**속성별 장단점**  
보습, 향, 트러블/자극, 발림성, 지속력, 가성비, 용기, 효과 — 8가지 항목별로 긍정 비율.  
전체 평점 4.9인데 트러블/자극 항목 긍정률이 33%인 상품이 실제로 있다.  
별점만 봐선 절대 알 수 없는 정보다.

**대안 상품 추천**  
이 상품의 가장 약한 속성에서 더 좋은 상품을 같은 카테고리 안에서 찾아서 보여준다.

**피부타입별 평가**  
무신사 리뷰에는 피부타입 정보가 있어서, 건성/지성/복합성/민감성별로 평가가 어떻게 다른지 볼 수 있다.

**리뷰 신뢰도**  
학습된 모델을 전체 리뷰에 돌려서 "텍스트 내용이 별점이랑 안 맞는다"고 판단된 리뷰가 얼마나 되는지 상품별로 보여준다.  
신뢰도가 낮은 상품에는 자동으로 경고를 띄운다.

---

## 모델이 실제로 하는 일

모델이 "별점과 내용이 안 맞는다"고 판단한 리뷰 **50,806건(전체의 9.5%)** 을  
속성별 집계에서 제외하고 나머지로 다시 계산했다.

앱 상단에 "이 숫자는 50,806건을 제외하고 계산됐습니다"가 표시된다.  
모델을 빼면 다른 숫자가 나온다.

이제 모델은 정확도 비교표에 숫자 채우는 역할이 아니라,  
**서비스에 보이는 숫자를 직접 바꾸는 역할**을 한다.

---

## 중간에 고친 것들

**형태소 분석 추가**  
"좋아요", "좋아서", "좋은데"가 전부 다른 단어로 취급되는 문제를 Okt로 해결했다.  
모델 정확도에는 별 차이 없었는데, 키워드 집계는 확실히 보기 좋아졌다.

**검색 개선**  
상품명 정확히 안 쳐도 찾을 수 있게 카테고리 필터, 브랜드명 검색, 여러 단어 AND 검색을 추가했다.

**카테고리 분류**  
무신사 원본에 카테고리 컬럼이 없어서 상품명 키워드로 추정하는 방식으로 채웠다.

**Streamlit 연결 끊기는 문제**  
앱을 열면 "연결 중..." 배너가 계속 떴다.  
Okt를 불러올 때 자바가 켜지면서 10초 정도 멈추는 게 원인이었다.  
처음 실제로 쓸 때만 켜도록 바꿨더니 해결됐다.
