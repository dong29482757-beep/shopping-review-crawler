# BeautyScope 개발 기록

화장품 리뷰 분석 서비스를 만들면서 겪은 것들 정리.  
피드백 받을 때마다 뭘 고쳤는지를 위주로 썼다.

---

## 처음 만든 것

쿠팡에서 화장품 리뷰를 긁어서 "이 리뷰가 긍정인지 부정인지 맞추는 모델"을 만들고,  
Streamlit으로 "리뷰 텍스트를 입력하면 감성을 알려주는" 웹 데모를 만들었다.

데이터는 세 군데에서 모았다.

| 플랫폼 | 건수 |
|---|---|
| 쿠팡 | 86,379건 |
| 무신사 | 230,870건 |
| 올리브영 | 221,525건 |
| **합계** | **538,774건** |

세 군데 데이터를 합치는 게 생각보다 복잡했다.  
쿠팡/무신사/올리브영이 컬럼 이름도 다르고 형식도 달랐고, 무신사는 CSV인데 올리브영은 JSONL 파일 4개였다.  
가장 골치 아팠던 건 **무신사의 긍정/부정 라벨이 별점 기준이 아니라 리뷰 텍스트 분석 기반**이었다는 것.  
5점짜리 리뷰인데 "부정"으로 라벨이 붙어있는 게 42,869건이나 됐다.  
결국 세 군데 데이터 전부 별점 기준으로 라벨을 다시 계산해서 통일했다 (4~5점=긍정, 3점=중립, 1~2점=부정).

---

## 모델 만들기

### 환경 때문에 numpy로 직접 구현

원래 PyTorch로 신경망을 짜려 했는데, 이 컴퓨터 파이썬이 3.14 버전이라 PyTorch가 설치가 안 됐다.  
공식 패키지 목록에 3.14용이 아직 없어서 그냥 오류만 떴다.

어쩔 수 없이 **numpy만 써서 신경망을 직접 구현**했다.  
처음엔 제약처럼 느껴졌는데, 역전파를 직접 짜보니까 내부 동작이 더 잘 이해됐다.

### 결과

| 모델 | 정확도 | 비고 |
|---|---|---|
| TF-IDF + 로지스틱 회귀 | 0.738 | 학습 6.6초 |
| numpy 직접 구현 신경망 | 0.762 | 학습 132초 |

---

## 1차 피드백: "LSTM이나 Transformer는 써봤냐"

비교 실험이 ML이랑 numpy 신경망 두 개뿐이라는 지적을 받았다.

그래서 Python 3.11을 따로 설치해서 별도 환경을 만들고 PyTorch를 깔았다.  
GPU가 있긴 한데 드라이버가 너무 오래돼서 최신 PyTorch랑 안 맞아서 CPU로 학습.

LSTM과 KoELECTRA(사전학습된 한국어 모델)를 추가로 돌렸다.

| 모델 | 정확도 | 학습 데이터 |
|---|---|---|
| TF-IDF + 로지스틱 회귀 | 0.738 | 136,150건 |
| numpy 신경망 | 0.762 | 136,150건 |
| LSTM (PyTorch) | 0.734 | 136,150건 |
| KoELECTRA | 0.772 | **20,000건만** |

예상 밖으로 LSTM이 TF-IDF 모델이랑 성능이 거의 똑같았다.  
짧은 화장품 리뷰는 단어 순서보다 어떤 단어를 쓰는지 자체가 더 강한 신호였던 것 같다.  
("별로", "최악", "재구매" 같은 단어가 이미 감성을 다 담고 있으니까)

KoELECTRA는 데이터를 1/7만 쓰고도 제일 높았다. 사전학습의 효과가 분명히 있었다.  
CPU라 시간이 너무 걸려서 전체 학습은 못 했는데, GPU 환경이었으면 격차가 더 벌어졌을 것.

---

## 2차 피드백: "불용어 처리 안 했다"

이때까지 텍스트를 자를 때 `re.findall(r"[가-힣]{2,}")` 이런 식으로 한글 2자 이상만 뽑았다.  
"좋아요", "좋아서", "좋은데"가 전부 다른 단어로 취급되는 구조였다.

Okt 형태소 분석기를 써서 명사/동사/형용사만 남기고,  
"제품", "사용", "구매" 같은 의미 없는 단어들은 직접 불용어 목록 만들어서 제거했다.

**그런데 솔직히 말하면, 이걸 적용해도 모델 정확도는 거의 안 올랐다.**  
오히려 살짝 떨어지는 경우도 있었다.  
Okt가 화장품 도메인 단어를 잘못 쪼개는 경우가 꽤 있었기 때문 ("재구매" → "재" + "구매" 이런 식으로).

대신 **키워드 집계는 확실히 좋아졌다.**  
이전엔 "좋다"가 "좋아요/좋아서/좋은데"로 분산돼서 순위가 낮게 나왔는데,  
형태소 분석 후엔 같은 어간으로 합쳐져서 실제 많이 쓰이는 단어가 제대로 보였다.

---

## 3차 피드백: "서비스가 실용성이 없다"

이게 제일 뼈아팠다.

"리뷰 텍스트를 직접 입력하면 긍정/부정 알려주는" 기능은  
실제로 화장품 사려는 사람한테 아무 쓸모가 없다.  
이미 써본 리뷰를 직접 타이핑해서 모델한테 물어볼 이유가 없으니까.

완전히 맞는 말이었다. 그래서 서비스를 갈아엎었다.

### 새로 만든 것: 상품 리포트

상품 이름으로 검색하면:
- **속성별 장단점** — 보습, 향, 트러블/자극, 발림성, 지속력, 가성비, 용기, 효과  
  8가지 항목별로 긍정 비율이 얼마나 되는지 보여준다
- **대안 상품 추천** — "이 상품의 가장 약한 속성이 더 좋은 상품" 자동 추천
- **피부타입별 평가** — 건성/지성/복합성/민감성별로 각 속성 평가가 어떤지
- **대표 리뷰** — 긍정/부정 리뷰 각 3개

전체 평점 4.9짜리 상품인데 "트러블/자극" 항목만 보면 부정 비율이 67%인 경우가 실제로 있다.  
그걸 보여주는 게 진짜 쓸모있는 서비스라고 판단했다.

### 속성 분석은 어떻게?

538만 건 전체를 ML 모델로 돌리면 Okt 형태소 분석이 병목이라 너무 오래 걸린다.  
(초당 약 200건이라 전체 돌리면 45분 이상)

그래서 속성 분석은 **키워드 매칭 방식**으로 만들었다. (`absa.py`)  
"보습", "촉촉", "수분" 같은 단어가 나오면 보습 속성, "따갑", "트러블", "뾰루지"가 나오면 트러블 속성,  
"안", "못", "별로" 같은 부정어가 앞에 오면 부정으로 분류하는 식.  
전체 데이터를 52초에 처리했다.

---

## 4차 피드백: "모델을 실제로 쓴 거냐"

맞는 말이었다. 모델을 만들어놓고 정확도 비교표에만 쓰고 있었다.  
ABSA 속성 분석은 규칙 기반(`absa.py`)이고, ML 모델은 서비스 어디에도 연결이 안 돼있었다.

그래서 이렇게 바꿨다.

### 리뷰 신뢰도 기능

별점 5점짜리 리뷰 중에 실제로는 부정적인 내용이 섞여 있는 경우가 있다.  
아까 데이터 통합할 때 봤던 것처럼, 무신사 데이터에서만 5점 리뷰의 21%가 실제론 부정 내용이었다.

학습된 ML 모델을 전체 리뷰 53만 건에 한 번 돌려서,  
**"모델이 텍스트만 보고 판단한 감성"이 별점과 얼마나 일치하는지**를 상품별로 집계했다.

이걸 "리뷰 신뢰도"라는 지표로 앱에 보여준다.  
신뢰도가 평균보다 많이 낮은 상품이면 "이 상품은 별점과 리뷰 내용이 안 맞는 경우가 많습니다" 경고를 띄운다.

처리 시간이 Okt 때문에 47분 정도 걸렸는데, 결과를 파일로 저장해두고 앱에서 불러쓰는 방식이라 매번 돌릴 필요는 없다.

결과적으로 모델이 단순 비교 실험용이 아니라, **서비스가 보여주는 숫자의 신뢰도를 뒤에서 검증하는 역할**을 하게 됐다.

### 기존 필터링에도 모델 반영

속성별 긍정 비율과 랭킹은 별점 기준 라벨로 계산한 건데,  
별점이 믿을 수 없는 리뷰가 섞여 있으면 그 숫자도 신뢰하기 어렵다.

그래서 ML 모델이 "별점과 리뷰 내용이 안 맞는다"고 판단한 리뷰 **50,806건 (전체의 9.5%)** 을  
속성 집계에서 제외하고 나머지 488,000건으로 재계산했다.

앱 상단에 "이 숫자는 50,806건을 제외하고 계산됐습니다"라고 표시된다.  
모델을 빼면 다른 숫자가 나온다.

---

## 기타 잡다한 것들

### 검색이 너무 불편했던 것

상품명 정확히 안 치면 검색이 안 됐다. 그래서 세 가지 고쳤다:
- **카테고리 필터** 추가 — 토너/세럼/선크림 등으로 먼저 좁히기
- **브랜드명 검색** 가능하게 — "닥터지"만 쳐도 나오게
- **여러 단어 AND 검색** — "닥터지 토너" 치면 두 단어 다 포함된 것만

### 무신사/올리브영 카테고리가 없었던 것

카테고리 필터를 만들었는데 쿠팡만 제대로 분류가 됐다.  
무신사 원본에는 카테고리 컬럼 자체가 없었고, 올리브영은 대분류 4개뿐이었다.

상품명에 들어간 키워드로 카테고리를 추정하는 방식으로 해결했다.  
"선크림"이 "크림"으로 잘못 분류되지 않게 키워드 순서를 잘 정해서.  
재실행 후 무신사 11종, 올리브영 14종으로 늘었다.

### Streamlit 연결이 끊기는 문제

앱을 열면 "연결 중..." 배너가 계속 떴다.  
원인은 `preprocessing_ko.py`가 불러와지는 순간 Okt를 바로 켜면서 자바가 뜨는데 (10초 걸림),  
그 동안 연결이 끊기는 거였다.  
Okt를 실제로 처음 쓸 때만 켜도록 바꿨더니 해결됐다.

---

## 최종 구조 요약

```
1_source/       소스코드 + 원본 데이터 (크롤러, 학습 코드, 전처리)
2_executable/   실행 파일 (python run.py 하면 바로 실행)
3_docs/         발표 피드백, 시연영상, 테크니컬 리포트
```

학습된 모델과 집계된 데이터는 `2_executable/models/`에 있어서  
다운로드 후 바로 실행 가능하고 다시 학습할 필요 없다.
